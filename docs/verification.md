# 验证记录

[中文首页](../README_zh.md) · [English](../README.md) · [创作记录](originality.md)

日期：**2026-09-08**。

## 本次代码与图片验证

- 原创实验实际运行，生成三张 SVG 及 `results.json`；默认预算支出 2,168 元、预留 432 元，分镜 600 帧，模拟验收通过 3/5 项。
- 三张 SVG 在本地浏览器加载、截图并逐张打开检查。新 PNG 是实际浏览器渲染结果，没有用 AI 重绘冒充运行截图。
- 图中数据为原创虚构资料，发布检查值来自资料文件，不是对真实网站采集的结果。分镜只是静态报告，没有导出视频。
- 旧的三张外部作品截图已从当前目录移除。未改写 Git 历史；原作者与原帖保留在社区延伸阅读。
- 检查了 12 版 README 的图片、语言导航、文档链接及代码块。默认首页仍为英文，各版保留 flaq.ai 与联盟计划说明。
- 校验文件记录生成程序、输入资料、SVG 和 PNG 的 SHA-256；离线测试确认提交的 SVG 与当前生成器输出一致。

## 离线测试

```bash
python3 -m unittest discover -s tests -v
node --test tests/test_javascript.mjs
```

结果：**22 项 Python 测试 + 6 项 JavaScript 测试通过，共 28 项**。

新增覆盖：人数变化导致的费用与预留金变化、连续帧区间及总时长不符、验收值严格类型比较、输入边界及重复编号、SVG 文本转义、输出重读与防覆盖、无效 JSON、API 附加资料与大小限制，以及 JavaScript 请求中的两项失败证据。原有 API 响应解析、图片编码、缺少密钥、拒绝、截断、HTTP 错误与保存测试继续通过。

网络响应由测试替身提供；测试不产生 API 费用。Python 和 Node.js 使用本机可用版本运行，建议使用 Python 3.10+ / Node.js 20+。

## 未验证的范围

- 未提交真实付费 API 请求；账号权限、在线兼容性、生成质量、延迟和费用尚未实测。
- 九个拓展任务是练习说明，没有全部实现；外部社区作品没有全面复现或独立验证。
- SVG 报告是固定 1280 × 720 教学布局，不是任意数据长度下的响应式产品；允许输入数量和文本长度有限制。
- 多语言说明尚未经过逐语言母语审校。

要补充真实调用验证，可运行 API 示例并保存 `--output`，记录输入、参数、时间与 `usage`。明确区分模型输出、本地计算和真实观测，不将私有输入或密钥提交到仓库。

## 微信文章与趣味指引更新

2026-09-08：浏览器读取微信原文及 Blender 官方 MCP、命令行页面，补充中英文趣味指引和原创 `examples/blender/desk_robot.py`。脚本只完成 Python 语法解析；本机未找到可用 Blender，未验证建模、动画、保存或导出。没有新增 Blender 运行截图或付费 API 调用。本次新增内容不计入上面的 28 项离线行为测试；原有测试结果的范围不变。

12 版 README 底部均添加原文标题、作者和来源链接，并保留 flaq.ai 与联盟计划。另检查本地链接、Markdown 代码块与语言导航。

## 客户端优先的文档调整

2026-09-08：12 版 README 改为 ChatGPT、Codex 客户端与 Codex CLI 三条直接使用入口；中文快速开始补全安装、登录、模型选择、材料提交、验收与排错。原 API 设置和费用说明迁入 `docs/api.md`，保留旧锚点跳转。原创实验与 Blender 指引增加直接在客户端发送任务的方式。

读取官方产品、模型、CLI 文档，并执行本机 `codex --help` 核对参数；没有安装客户端、发起登录、修改账号设置或运行新模型会话。文档链接、语言导航和代码块已检查；此次没有修改既有程序，无需扩大此前 28 项行为测试的验证范围。

## X 场景扩充（2026-09-22）

- 在 X 搜索并逐条读取七条原帖，另读取场景迁移作者的资产来源与用量回复。具体 URL、显示日期、归纳和验证边界见 [清单](research/x-sources-2026-09-22.json)。
- 新增八个任务说明；字幕和调色共享一条来源。没有完整观看全部视频、复现原作、调用收费服务或导入真实业务数据。
- 12 版 README 增加本地化入口；英文概览与中文详细练习位于同一指引。原有 flaq.ai、联盟说明、微信原文及原创截图保留。
- 新增内容为文档与来源数据，不增加此前 28 项程序测试的验证范围。检查 Markdown 链接、锚点、代码块、语言导航、来源 URL 一致性和来源 JSON 格式；没有修改现有程序。
