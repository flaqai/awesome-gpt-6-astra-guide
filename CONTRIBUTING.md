# 参与完善指南

欢迎修正链接、更新官方参数、补充可复现案例与多语言说明。

新增原创案例请提供：输入资料、源代码、运行命令、输出和验收标准。外部案例请放入 `docs/community-references.md`，保留作者和原帖链接。不要把转载、模拟输出或自己推测的提示词当成原始证据。

展示区优先使用本项目原创代码运行后采集的截图；同时保留生成源文件、输入资料和 `assets/screenshots/manifest.json` 校验值。新增截图请在 `assets/screenshots/README.md` 记录日期、生成方式与权利归属。不要提交密钥、个人账号信息或私有素材。第三方素材不会自动适用本仓库许可证。

代码改动后运行：

```bash
python3 -m unittest discover -s tests -v
node --test tests/test_javascript.mjs
```

文档更新后检查相对路径和图片。官方规格变化时，同时更新首页、相关章节与资料核对日期；未做真实 API 调用时，保留这一说明。

## 多语言 README 维护

默认入口为英文 `README.md`；另有简体中文、繁体中文、日语、韩语、西班牙语、法语、德语、葡萄牙语、俄语、印尼语与阿拉伯语版本，使用 `README_<语言代码>.md` 命名。英文首页是后续内容同步的基准。

更新事实、示例模式、截图、团队介绍或联盟条款时，同步检查所有版本。各 README 应保留完整语言导航、当地语言的入门指令、代码入口、案例与来源说明、flaq.ai 介绍及联盟支持。详细教程尚未翻译时，明确告知其语言。

新增语言时更新所有 README 的导航，在新文件中使用相同的相对路径，并检查本地链接和代码块。截图中的作品权利及实际验证范围不能因翻译而扩大。
